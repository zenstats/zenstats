// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/monthlyeventcount"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/user"
)

// MonthlyEventCount is the model entity for the MonthlyEventCount schema.
type MonthlyEventCount struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// 关联用户ID
	UserID int64 `json:"user_id,omitempty"`
	// 年份
	Year int `json:"year,omitempty"`
	// 月份
	Month int `json:"month,omitempty"`
	// 事件数
	Count int64 `json:"count,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MonthlyEventCountQuery when eager-loading is set.
	Edges        MonthlyEventCountEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MonthlyEventCountEdges holds the relations/edges for other nodes in the graph.
type MonthlyEventCountEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MonthlyEventCountEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MonthlyEventCount) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case monthlyeventcount.FieldID, monthlyeventcount.FieldUserID, monthlyeventcount.FieldYear, monthlyeventcount.FieldMonth, monthlyeventcount.FieldCount:
			values[i] = new(sql.NullInt64)
		case monthlyeventcount.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MonthlyEventCount fields.
func (mec *MonthlyEventCount) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case monthlyeventcount.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			mec.ID = int64(value.Int64)
		case monthlyeventcount.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				mec.UserID = value.Int64
			}
		case monthlyeventcount.FieldYear:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field year", values[i])
			} else if value.Valid {
				mec.Year = int(value.Int64)
			}
		case monthlyeventcount.FieldMonth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field month", values[i])
			} else if value.Valid {
				mec.Month = int(value.Int64)
			}
		case monthlyeventcount.FieldCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field count", values[i])
			} else if value.Valid {
				mec.Count = value.Int64
			}
		case monthlyeventcount.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				mec.UpdatedAt = value.Time
			}
		default:
			mec.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MonthlyEventCount.
// This includes values selected through modifiers, order, etc.
func (mec *MonthlyEventCount) Value(name string) (ent.Value, error) {
	return mec.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the MonthlyEventCount entity.
func (mec *MonthlyEventCount) QueryUser() *UserQuery {
	return NewMonthlyEventCountClient(mec.config).QueryUser(mec)
}

// Update returns a builder for updating this MonthlyEventCount.
// Note that you need to call MonthlyEventCount.Unwrap() before calling this method if this MonthlyEventCount
// was returned from a transaction, and the transaction was committed or rolled back.
func (mec *MonthlyEventCount) Update() *MonthlyEventCountUpdateOne {
	return NewMonthlyEventCountClient(mec.config).UpdateOne(mec)
}

// Unwrap unwraps the MonthlyEventCount entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mec *MonthlyEventCount) Unwrap() *MonthlyEventCount {
	_tx, ok := mec.config.driver.(*txDriver)
	if !ok {
		panic("ent: MonthlyEventCount is not a transactional entity")
	}
	mec.config.driver = _tx.drv
	return mec
}

// String implements the fmt.Stringer.
func (mec *MonthlyEventCount) String() string {
	var builder strings.Builder
	builder.WriteString("MonthlyEventCount(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mec.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", mec.UserID))
	builder.WriteString(", ")
	builder.WriteString("year=")
	builder.WriteString(fmt.Sprintf("%v", mec.Year))
	builder.WriteString(", ")
	builder.WriteString("month=")
	builder.WriteString(fmt.Sprintf("%v", mec.Month))
	builder.WriteString(", ")
	builder.WriteString("count=")
	builder.WriteString(fmt.Sprintf("%v", mec.Count))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(mec.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MonthlyEventCounts is a parsable slice of MonthlyEventCount.
type MonthlyEventCounts []*MonthlyEventCount
