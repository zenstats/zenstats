// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/schema"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/shieldrulesip"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// ShieldRulesIp is the model entity for the ShieldRulesIp schema.
type ShieldRulesIp struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// SiteID holds the value of the "site_id" field.
	SiteID int64 `json:"site_id,omitempty"`
	// Inet holds the value of the "inet" field.
	Inet *schema.Inet `json:"inet,omitempty"`
	// Action holds the value of the "action" field.
	Action string `json:"action,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// AddedBy holds the value of the "added_by" field.
	AddedBy string `json:"added_by,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShieldRulesIpQuery when eager-loading is set.
	Edges        ShieldRulesIpEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ShieldRulesIpEdges holds the relations/edges for other nodes in the graph.
type ShieldRulesIpEdges struct {
	// Site holds the value of the site edge.
	Site *Site `json:"site,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SiteOrErr returns the Site value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShieldRulesIpEdges) SiteOrErr() (*Site, error) {
	if e.Site != nil {
		return e.Site, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: site.Label}
	}
	return nil, &NotLoadedError{edge: "site"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShieldRulesIp) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case shieldrulesip.FieldInet:
			values[i] = new(schema.Inet)
		case shieldrulesip.FieldID, shieldrulesip.FieldSiteID:
			values[i] = new(sql.NullInt64)
		case shieldrulesip.FieldAction, shieldrulesip.FieldDescription, shieldrulesip.FieldAddedBy:
			values[i] = new(sql.NullString)
		case shieldrulesip.FieldCreatedAt, shieldrulesip.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShieldRulesIp fields.
func (sri *ShieldRulesIp) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shieldrulesip.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sri.ID = int64(value.Int64)
		case shieldrulesip.FieldSiteID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field site_id", values[i])
			} else if value.Valid {
				sri.SiteID = value.Int64
			}
		case shieldrulesip.FieldInet:
			if value, ok := values[i].(*schema.Inet); !ok {
				return fmt.Errorf("unexpected type %T for field inet", values[i])
			} else if value != nil {
				sri.Inet = value
			}
		case shieldrulesip.FieldAction:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field action", values[i])
			} else if value.Valid {
				sri.Action = value.String
			}
		case shieldrulesip.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				sri.Description = value.String
			}
		case shieldrulesip.FieldAddedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field added_by", values[i])
			} else if value.Valid {
				sri.AddedBy = value.String
			}
		case shieldrulesip.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sri.CreatedAt = value.Time
			}
		case shieldrulesip.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sri.UpdatedAt = value.Time
			}
		default:
			sri.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ShieldRulesIp.
// This includes values selected through modifiers, order, etc.
func (sri *ShieldRulesIp) Value(name string) (ent.Value, error) {
	return sri.selectValues.Get(name)
}

// QuerySite queries the "site" edge of the ShieldRulesIp entity.
func (sri *ShieldRulesIp) QuerySite() *SiteQuery {
	return NewShieldRulesIpClient(sri.config).QuerySite(sri)
}

// Update returns a builder for updating this ShieldRulesIp.
// Note that you need to call ShieldRulesIp.Unwrap() before calling this method if this ShieldRulesIp
// was returned from a transaction, and the transaction was committed or rolled back.
func (sri *ShieldRulesIp) Update() *ShieldRulesIpUpdateOne {
	return NewShieldRulesIpClient(sri.config).UpdateOne(sri)
}

// Unwrap unwraps the ShieldRulesIp entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sri *ShieldRulesIp) Unwrap() *ShieldRulesIp {
	_tx, ok := sri.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShieldRulesIp is not a transactional entity")
	}
	sri.config.driver = _tx.drv
	return sri
}

// String implements the fmt.Stringer.
func (sri *ShieldRulesIp) String() string {
	var builder strings.Builder
	builder.WriteString("ShieldRulesIp(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sri.ID))
	builder.WriteString("site_id=")
	builder.WriteString(fmt.Sprintf("%v", sri.SiteID))
	builder.WriteString(", ")
	builder.WriteString("inet=")
	builder.WriteString(fmt.Sprintf("%v", sri.Inet))
	builder.WriteString(", ")
	builder.WriteString("action=")
	builder.WriteString(sri.Action)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(sri.Description)
	builder.WriteString(", ")
	builder.WriteString("added_by=")
	builder.WriteString(sri.AddedBy)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sri.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sri.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ShieldRulesIps is a parsable slice of ShieldRulesIp.
type ShieldRulesIps []*ShieldRulesIp
