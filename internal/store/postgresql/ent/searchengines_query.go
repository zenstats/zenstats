// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/predicate"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/searchengines"
)

// SearchEnginesQuery is the builder for querying SearchEngines entities.
type SearchEnginesQuery struct {
	config
	ctx        *QueryContext
	order      []searchengines.OrderOption
	inters     []Interceptor
	predicates []predicate.SearchEngines
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SearchEnginesQuery builder.
func (seq *SearchEnginesQuery) Where(ps ...predicate.SearchEngines) *SearchEnginesQuery {
	seq.predicates = append(seq.predicates, ps...)
	return seq
}

// Limit the number of records to be returned by this query.
func (seq *SearchEnginesQuery) Limit(limit int) *SearchEnginesQuery {
	seq.ctx.Limit = &limit
	return seq
}

// Offset to start from.
func (seq *SearchEnginesQuery) Offset(offset int) *SearchEnginesQuery {
	seq.ctx.Offset = &offset
	return seq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (seq *SearchEnginesQuery) Unique(unique bool) *SearchEnginesQuery {
	seq.ctx.Unique = &unique
	return seq
}

// Order specifies how the records should be ordered.
func (seq *SearchEnginesQuery) Order(o ...searchengines.OrderOption) *SearchEnginesQuery {
	seq.order = append(seq.order, o...)
	return seq
}

// First returns the first SearchEngines entity from the query.
// Returns a *NotFoundError when no SearchEngines was found.
func (seq *SearchEnginesQuery) First(ctx context.Context) (*SearchEngines, error) {
	nodes, err := seq.Limit(1).All(setContextOp(ctx, seq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{searchengines.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (seq *SearchEnginesQuery) FirstX(ctx context.Context) *SearchEngines {
	node, err := seq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SearchEngines ID from the query.
// Returns a *NotFoundError when no SearchEngines ID was found.
func (seq *SearchEnginesQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = seq.Limit(1).IDs(setContextOp(ctx, seq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{searchengines.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (seq *SearchEnginesQuery) FirstIDX(ctx context.Context) int64 {
	id, err := seq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SearchEngines entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SearchEngines entity is found.
// Returns a *NotFoundError when no SearchEngines entities are found.
func (seq *SearchEnginesQuery) Only(ctx context.Context) (*SearchEngines, error) {
	nodes, err := seq.Limit(2).All(setContextOp(ctx, seq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{searchengines.Label}
	default:
		return nil, &NotSingularError{searchengines.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (seq *SearchEnginesQuery) OnlyX(ctx context.Context) *SearchEngines {
	node, err := seq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SearchEngines ID in the query.
// Returns a *NotSingularError when more than one SearchEngines ID is found.
// Returns a *NotFoundError when no entities are found.
func (seq *SearchEnginesQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = seq.Limit(2).IDs(setContextOp(ctx, seq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{searchengines.Label}
	default:
		err = &NotSingularError{searchengines.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (seq *SearchEnginesQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := seq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SearchEnginesSlice.
func (seq *SearchEnginesQuery) All(ctx context.Context) ([]*SearchEngines, error) {
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryAll)
	if err := seq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SearchEngines, *SearchEnginesQuery]()
	return withInterceptors[[]*SearchEngines](ctx, seq, qr, seq.inters)
}

// AllX is like All, but panics if an error occurs.
func (seq *SearchEnginesQuery) AllX(ctx context.Context) []*SearchEngines {
	nodes, err := seq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SearchEngines IDs.
func (seq *SearchEnginesQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if seq.ctx.Unique == nil && seq.path != nil {
		seq.Unique(true)
	}
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryIDs)
	if err = seq.Select(searchengines.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (seq *SearchEnginesQuery) IDsX(ctx context.Context) []int64 {
	ids, err := seq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (seq *SearchEnginesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryCount)
	if err := seq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, seq, querierCount[*SearchEnginesQuery](), seq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (seq *SearchEnginesQuery) CountX(ctx context.Context) int {
	count, err := seq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (seq *SearchEnginesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryExist)
	switch _, err := seq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (seq *SearchEnginesQuery) ExistX(ctx context.Context) bool {
	exist, err := seq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SearchEnginesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (seq *SearchEnginesQuery) Clone() *SearchEnginesQuery {
	if seq == nil {
		return nil
	}
	return &SearchEnginesQuery{
		config:     seq.config,
		ctx:        seq.ctx.Clone(),
		order:      append([]searchengines.OrderOption{}, seq.order...),
		inters:     append([]Interceptor{}, seq.inters...),
		predicates: append([]predicate.SearchEngines{}, seq.predicates...),
		// clone intermediate query.
		sql:  seq.sql.Clone(),
		path: seq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Domain string `json:"domain,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SearchEngines.Query().
//		GroupBy(searchengines.FieldDomain).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (seq *SearchEnginesQuery) GroupBy(field string, fields ...string) *SearchEnginesGroupBy {
	seq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SearchEnginesGroupBy{build: seq}
	grbuild.flds = &seq.ctx.Fields
	grbuild.label = searchengines.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Domain string `json:"domain,omitempty"`
//	}
//
//	client.SearchEngines.Query().
//		Select(searchengines.FieldDomain).
//		Scan(ctx, &v)
func (seq *SearchEnginesQuery) Select(fields ...string) *SearchEnginesSelect {
	seq.ctx.Fields = append(seq.ctx.Fields, fields...)
	sbuild := &SearchEnginesSelect{SearchEnginesQuery: seq}
	sbuild.label = searchengines.Label
	sbuild.flds, sbuild.scan = &seq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SearchEnginesSelect configured with the given aggregations.
func (seq *SearchEnginesQuery) Aggregate(fns ...AggregateFunc) *SearchEnginesSelect {
	return seq.Select().Aggregate(fns...)
}

func (seq *SearchEnginesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range seq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, seq); err != nil {
				return err
			}
		}
	}
	for _, f := range seq.ctx.Fields {
		if !searchengines.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if seq.path != nil {
		prev, err := seq.path(ctx)
		if err != nil {
			return err
		}
		seq.sql = prev
	}
	return nil
}

func (seq *SearchEnginesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SearchEngines, error) {
	var (
		nodes = []*SearchEngines{}
		_spec = seq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SearchEngines).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SearchEngines{config: seq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, seq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (seq *SearchEnginesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := seq.querySpec()
	_spec.Node.Columns = seq.ctx.Fields
	if len(seq.ctx.Fields) > 0 {
		_spec.Unique = seq.ctx.Unique != nil && *seq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, seq.driver, _spec)
}

func (seq *SearchEnginesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(searchengines.Table, searchengines.Columns, sqlgraph.NewFieldSpec(searchengines.FieldID, field.TypeInt64))
	_spec.From = seq.sql
	if unique := seq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if seq.path != nil {
		_spec.Unique = true
	}
	if fields := seq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, searchengines.FieldID)
		for i := range fields {
			if fields[i] != searchengines.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := seq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := seq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := seq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := seq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (seq *SearchEnginesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(seq.driver.Dialect())
	t1 := builder.Table(searchengines.Table)
	columns := seq.ctx.Fields
	if len(columns) == 0 {
		columns = searchengines.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if seq.sql != nil {
		selector = seq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if seq.ctx.Unique != nil && *seq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range seq.predicates {
		p(selector)
	}
	for _, p := range seq.order {
		p(selector)
	}
	if offset := seq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := seq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SearchEnginesGroupBy is the group-by builder for SearchEngines entities.
type SearchEnginesGroupBy struct {
	selector
	build *SearchEnginesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (segb *SearchEnginesGroupBy) Aggregate(fns ...AggregateFunc) *SearchEnginesGroupBy {
	segb.fns = append(segb.fns, fns...)
	return segb
}

// Scan applies the selector query and scans the result into the given value.
func (segb *SearchEnginesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, segb.build.ctx, ent.OpQueryGroupBy)
	if err := segb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SearchEnginesQuery, *SearchEnginesGroupBy](ctx, segb.build, segb, segb.build.inters, v)
}

func (segb *SearchEnginesGroupBy) sqlScan(ctx context.Context, root *SearchEnginesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(segb.fns))
	for _, fn := range segb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*segb.flds)+len(segb.fns))
		for _, f := range *segb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*segb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := segb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SearchEnginesSelect is the builder for selecting fields of SearchEngines entities.
type SearchEnginesSelect struct {
	*SearchEnginesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ses *SearchEnginesSelect) Aggregate(fns ...AggregateFunc) *SearchEnginesSelect {
	ses.fns = append(ses.fns, fns...)
	return ses
}

// Scan applies the selector query and scans the result into the given value.
func (ses *SearchEnginesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ses.ctx, ent.OpQuerySelect)
	if err := ses.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SearchEnginesQuery, *SearchEnginesSelect](ctx, ses.SearchEnginesQuery, ses, ses.inters, v)
}

func (ses *SearchEnginesSelect) sqlScan(ctx context.Context, root *SearchEnginesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ses.fns))
	for _, fn := range ses.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ses.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ses.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
