// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/funnel"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/funnelstep"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/predicate"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// FunnelQuery is the builder for querying Funnel entities.
type FunnelQuery struct {
	config
	ctx             *QueryContext
	order           []funnel.OrderOption
	inters          []Interceptor
	predicates      []predicate.Funnel
	withSite        *SiteQuery
	withFunnelSteps *FunnelStepQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FunnelQuery builder.
func (fq *FunnelQuery) Where(ps ...predicate.Funnel) *FunnelQuery {
	fq.predicates = append(fq.predicates, ps...)
	return fq
}

// Limit the number of records to be returned by this query.
func (fq *FunnelQuery) Limit(limit int) *FunnelQuery {
	fq.ctx.Limit = &limit
	return fq
}

// Offset to start from.
func (fq *FunnelQuery) Offset(offset int) *FunnelQuery {
	fq.ctx.Offset = &offset
	return fq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fq *FunnelQuery) Unique(unique bool) *FunnelQuery {
	fq.ctx.Unique = &unique
	return fq
}

// Order specifies how the records should be ordered.
func (fq *FunnelQuery) Order(o ...funnel.OrderOption) *FunnelQuery {
	fq.order = append(fq.order, o...)
	return fq
}

// QuerySite chains the current query on the "site" edge.
func (fq *FunnelQuery) QuerySite() *SiteQuery {
	query := (&SiteClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(funnel.Table, funnel.FieldID, selector),
			sqlgraph.To(site.Table, site.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, funnel.SiteTable, funnel.SiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFunnelSteps chains the current query on the "funnel_steps" edge.
func (fq *FunnelQuery) QueryFunnelSteps() *FunnelStepQuery {
	query := (&FunnelStepClient{config: fq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(funnel.Table, funnel.FieldID, selector),
			sqlgraph.To(funnelstep.Table, funnelstep.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, funnel.FunnelStepsTable, funnel.FunnelStepsColumn),
		)
		fromU = sqlgraph.SetNeighbors(fq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Funnel entity from the query.
// Returns a *NotFoundError when no Funnel was found.
func (fq *FunnelQuery) First(ctx context.Context) (*Funnel, error) {
	nodes, err := fq.Limit(1).All(setContextOp(ctx, fq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{funnel.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fq *FunnelQuery) FirstX(ctx context.Context) *Funnel {
	node, err := fq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Funnel ID from the query.
// Returns a *NotFoundError when no Funnel ID was found.
func (fq *FunnelQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fq.Limit(1).IDs(setContextOp(ctx, fq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{funnel.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fq *FunnelQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Funnel entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Funnel entity is found.
// Returns a *NotFoundError when no Funnel entities are found.
func (fq *FunnelQuery) Only(ctx context.Context) (*Funnel, error) {
	nodes, err := fq.Limit(2).All(setContextOp(ctx, fq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{funnel.Label}
	default:
		return nil, &NotSingularError{funnel.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fq *FunnelQuery) OnlyX(ctx context.Context) *Funnel {
	node, err := fq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Funnel ID in the query.
// Returns a *NotSingularError when more than one Funnel ID is found.
// Returns a *NotFoundError when no entities are found.
func (fq *FunnelQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fq.Limit(2).IDs(setContextOp(ctx, fq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{funnel.Label}
	default:
		err = &NotSingularError{funnel.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fq *FunnelQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Funnels.
func (fq *FunnelQuery) All(ctx context.Context) ([]*Funnel, error) {
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryAll)
	if err := fq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Funnel, *FunnelQuery]()
	return withInterceptors[[]*Funnel](ctx, fq, qr, fq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fq *FunnelQuery) AllX(ctx context.Context) []*Funnel {
	nodes, err := fq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Funnel IDs.
func (fq *FunnelQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fq.ctx.Unique == nil && fq.path != nil {
		fq.Unique(true)
	}
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryIDs)
	if err = fq.Select(funnel.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fq *FunnelQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fq *FunnelQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryCount)
	if err := fq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fq, querierCount[*FunnelQuery](), fq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fq *FunnelQuery) CountX(ctx context.Context) int {
	count, err := fq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fq *FunnelQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fq.ctx, ent.OpQueryExist)
	switch _, err := fq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fq *FunnelQuery) ExistX(ctx context.Context) bool {
	exist, err := fq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FunnelQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fq *FunnelQuery) Clone() *FunnelQuery {
	if fq == nil {
		return nil
	}
	return &FunnelQuery{
		config:          fq.config,
		ctx:             fq.ctx.Clone(),
		order:           append([]funnel.OrderOption{}, fq.order...),
		inters:          append([]Interceptor{}, fq.inters...),
		predicates:      append([]predicate.Funnel{}, fq.predicates...),
		withSite:        fq.withSite.Clone(),
		withFunnelSteps: fq.withFunnelSteps.Clone(),
		// clone intermediate query.
		sql:  fq.sql.Clone(),
		path: fq.path,
	}
}

// WithSite tells the query-builder to eager-load the nodes that are connected to
// the "site" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FunnelQuery) WithSite(opts ...func(*SiteQuery)) *FunnelQuery {
	query := (&SiteClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withSite = query
	return fq
}

// WithFunnelSteps tells the query-builder to eager-load the nodes that are connected to
// the "funnel_steps" edge. The optional arguments are used to configure the query builder of the edge.
func (fq *FunnelQuery) WithFunnelSteps(opts ...func(*FunnelStepQuery)) *FunnelQuery {
	query := (&FunnelStepClient{config: fq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fq.withFunnelSteps = query
	return fq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Funnel.Query().
//		GroupBy(funnel.FieldSiteID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fq *FunnelQuery) GroupBy(field string, fields ...string) *FunnelGroupBy {
	fq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FunnelGroupBy{build: fq}
	grbuild.flds = &fq.ctx.Fields
	grbuild.label = funnel.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//	}
//
//	client.Funnel.Query().
//		Select(funnel.FieldSiteID).
//		Scan(ctx, &v)
func (fq *FunnelQuery) Select(fields ...string) *FunnelSelect {
	fq.ctx.Fields = append(fq.ctx.Fields, fields...)
	sbuild := &FunnelSelect{FunnelQuery: fq}
	sbuild.label = funnel.Label
	sbuild.flds, sbuild.scan = &fq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FunnelSelect configured with the given aggregations.
func (fq *FunnelQuery) Aggregate(fns ...AggregateFunc) *FunnelSelect {
	return fq.Select().Aggregate(fns...)
}

func (fq *FunnelQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fq); err != nil {
				return err
			}
		}
	}
	for _, f := range fq.ctx.Fields {
		if !funnel.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fq.path != nil {
		prev, err := fq.path(ctx)
		if err != nil {
			return err
		}
		fq.sql = prev
	}
	return nil
}

func (fq *FunnelQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Funnel, error) {
	var (
		nodes       = []*Funnel{}
		_spec       = fq.querySpec()
		loadedTypes = [2]bool{
			fq.withSite != nil,
			fq.withFunnelSteps != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Funnel).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Funnel{config: fq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fq.withSite; query != nil {
		if err := fq.loadSite(ctx, query, nodes, nil,
			func(n *Funnel, e *Site) { n.Edges.Site = e }); err != nil {
			return nil, err
		}
	}
	if query := fq.withFunnelSteps; query != nil {
		if err := fq.loadFunnelSteps(ctx, query, nodes,
			func(n *Funnel) { n.Edges.FunnelSteps = []*FunnelStep{} },
			func(n *Funnel, e *FunnelStep) { n.Edges.FunnelSteps = append(n.Edges.FunnelSteps, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fq *FunnelQuery) loadSite(ctx context.Context, query *SiteQuery, nodes []*Funnel, init func(*Funnel), assign func(*Funnel, *Site)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*Funnel)
	for i := range nodes {
		fk := nodes[i].SiteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(site.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fq *FunnelQuery) loadFunnelSteps(ctx context.Context, query *FunnelStepQuery, nodes []*Funnel, init func(*Funnel), assign func(*Funnel, *FunnelStep)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*Funnel)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(funnelstep.FieldFunnelID)
	}
	query.Where(predicate.FunnelStep(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(funnel.FunnelStepsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.FunnelID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "funnel_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (fq *FunnelQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fq.querySpec()
	_spec.Node.Columns = fq.ctx.Fields
	if len(fq.ctx.Fields) > 0 {
		_spec.Unique = fq.ctx.Unique != nil && *fq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fq.driver, _spec)
}

func (fq *FunnelQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(funnel.Table, funnel.Columns, sqlgraph.NewFieldSpec(funnel.FieldID, field.TypeInt64))
	_spec.From = fq.sql
	if unique := fq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fq.path != nil {
		_spec.Unique = true
	}
	if fields := fq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, funnel.FieldID)
		for i := range fields {
			if fields[i] != funnel.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if fq.withSite != nil {
			_spec.Node.AddColumnOnce(funnel.FieldSiteID)
		}
	}
	if ps := fq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fq *FunnelQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fq.driver.Dialect())
	t1 := builder.Table(funnel.Table)
	columns := fq.ctx.Fields
	if len(columns) == 0 {
		columns = funnel.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fq.sql != nil {
		selector = fq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fq.ctx.Unique != nil && *fq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fq.predicates {
		p(selector)
	}
	for _, p := range fq.order {
		p(selector)
	}
	if offset := fq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FunnelGroupBy is the group-by builder for Funnel entities.
type FunnelGroupBy struct {
	selector
	build *FunnelQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fgb *FunnelGroupBy) Aggregate(fns ...AggregateFunc) *FunnelGroupBy {
	fgb.fns = append(fgb.fns, fns...)
	return fgb
}

// Scan applies the selector query and scans the result into the given value.
func (fgb *FunnelGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fgb.build.ctx, ent.OpQueryGroupBy)
	if err := fgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FunnelQuery, *FunnelGroupBy](ctx, fgb.build, fgb, fgb.build.inters, v)
}

func (fgb *FunnelGroupBy) sqlScan(ctx context.Context, root *FunnelQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fgb.fns))
	for _, fn := range fgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fgb.flds)+len(fgb.fns))
		for _, f := range *fgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FunnelSelect is the builder for selecting fields of Funnel entities.
type FunnelSelect struct {
	*FunnelQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fs *FunnelSelect) Aggregate(fns ...AggregateFunc) *FunnelSelect {
	fs.fns = append(fs.fns, fns...)
	return fs
}

// Scan applies the selector query and scans the result into the given value.
func (fs *FunnelSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fs.ctx, ent.OpQuerySelect)
	if err := fs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FunnelQuery, *FunnelSelect](ctx, fs.FunnelQuery, fs, fs.inters, v)
}

func (fs *FunnelSelect) sqlScan(ctx context.Context, root *FunnelQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fs.fns))
	for _, fn := range fs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
