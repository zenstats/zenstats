// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/funnel"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// Funnel is the model entity for the Funnel schema.
type Funnel struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// SiteID holds the value of the "site_id" field.
	SiteID int64 `json:"site_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FunnelQuery when eager-loading is set.
	Edges        FunnelEdges `json:"edges"`
	selectValues sql.SelectValues
}

// FunnelEdges holds the relations/edges for other nodes in the graph.
type FunnelEdges struct {
	// Site holds the value of the site edge.
	Site *Site `json:"site,omitempty"`
	// FunnelSteps holds the value of the funnel_steps edge.
	FunnelSteps []*FunnelStep `json:"funnel_steps,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SiteOrErr returns the Site value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FunnelEdges) SiteOrErr() (*Site, error) {
	if e.Site != nil {
		return e.Site, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: site.Label}
	}
	return nil, &NotLoadedError{edge: "site"}
}

// FunnelStepsOrErr returns the FunnelSteps value or an error if the edge
// was not loaded in eager-loading.
func (e FunnelEdges) FunnelStepsOrErr() ([]*FunnelStep, error) {
	if e.loadedTypes[1] {
		return e.FunnelSteps, nil
	}
	return nil, &NotLoadedError{edge: "funnel_steps"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Funnel) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case funnel.FieldID, funnel.FieldSiteID:
			values[i] = new(sql.NullInt64)
		case funnel.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Funnel fields.
func (f *Funnel) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case funnel.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			f.ID = int64(value.Int64)
		case funnel.FieldSiteID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field site_id", values[i])
			} else if value.Valid {
				f.SiteID = value.Int64
			}
		case funnel.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				f.Name = value.String
			}
		default:
			f.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Funnel.
// This includes values selected through modifiers, order, etc.
func (f *Funnel) Value(name string) (ent.Value, error) {
	return f.selectValues.Get(name)
}

// QuerySite queries the "site" edge of the Funnel entity.
func (f *Funnel) QuerySite() *SiteQuery {
	return NewFunnelClient(f.config).QuerySite(f)
}

// QueryFunnelSteps queries the "funnel_steps" edge of the Funnel entity.
func (f *Funnel) QueryFunnelSteps() *FunnelStepQuery {
	return NewFunnelClient(f.config).QueryFunnelSteps(f)
}

// Update returns a builder for updating this Funnel.
// Note that you need to call Funnel.Unwrap() before calling this method if this Funnel
// was returned from a transaction, and the transaction was committed or rolled back.
func (f *Funnel) Update() *FunnelUpdateOne {
	return NewFunnelClient(f.config).UpdateOne(f)
}

// Unwrap unwraps the Funnel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (f *Funnel) Unwrap() *Funnel {
	_tx, ok := f.config.driver.(*txDriver)
	if !ok {
		panic("ent: Funnel is not a transactional entity")
	}
	f.config.driver = _tx.drv
	return f
}

// String implements the fmt.Stringer.
func (f *Funnel) String() string {
	var builder strings.Builder
	builder.WriteString("Funnel(")
	builder.WriteString(fmt.Sprintf("id=%v, ", f.ID))
	builder.WriteString("site_id=")
	builder.WriteString(fmt.Sprintf("%v", f.SiteID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(f.Name)
	builder.WriteByte(')')
	return builder.String()
}

// Funnels is a parsable slice of Funnel.
type Funnels []*Funnel
