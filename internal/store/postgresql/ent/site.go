// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// Site is the model entity for the Site schema.
type Site struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Domain holds the value of the "domain" field.
	Domain string `json:"domain,omitempty"`
	// Timezone holds the value of the "timezone" field.
	Timezone string `json:"timezone,omitempty"`
	// Public holds the value of the "public" field.
	Public bool `json:"public,omitempty"`
	// StatsStartDate holds the value of the "stats_start_date" field.
	StatsStartDate *time.Time `json:"stats_start_date,omitempty"`
	// IngestRateLimitScaleSeconds holds the value of the "ingest_rate_limit_scale_seconds" field.
	IngestRateLimitScaleSeconds int `json:"ingest_rate_limit_scale_seconds,omitempty"`
	// IngestLimitPerMinute holds the value of the "ingest_limit_per_minute" field.
	IngestLimitPerMinute int `json:"ingest_limit_per_minute,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SiteQuery when eager-loading is set.
	Edges        SiteEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SiteEdges holds the relations/edges for other nodes in the graph.
type SiteEdges struct {
	// Funnels holds the value of the funnels edge.
	Funnels []*Funnel `json:"funnels,omitempty"`
	// Members holds the value of the members edge.
	Members []*User `json:"members,omitempty"`
	// Goals holds the value of the goals edge.
	Goals []*Goal `json:"goals,omitempty"`
	// SiteMemberships holds the value of the site_memberships edge.
	SiteMemberships []*SiteMembership `json:"site_memberships,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// FunnelsOrErr returns the Funnels value or an error if the edge
// was not loaded in eager-loading.
func (e SiteEdges) FunnelsOrErr() ([]*Funnel, error) {
	if e.loadedTypes[0] {
		return e.Funnels, nil
	}
	return nil, &NotLoadedError{edge: "funnels"}
}

// MembersOrErr returns the Members value or an error if the edge
// was not loaded in eager-loading.
func (e SiteEdges) MembersOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.Members, nil
	}
	return nil, &NotLoadedError{edge: "members"}
}

// GoalsOrErr returns the Goals value or an error if the edge
// was not loaded in eager-loading.
func (e SiteEdges) GoalsOrErr() ([]*Goal, error) {
	if e.loadedTypes[2] {
		return e.Goals, nil
	}
	return nil, &NotLoadedError{edge: "goals"}
}

// SiteMembershipsOrErr returns the SiteMemberships value or an error if the edge
// was not loaded in eager-loading.
func (e SiteEdges) SiteMembershipsOrErr() ([]*SiteMembership, error) {
	if e.loadedTypes[3] {
		return e.SiteMemberships, nil
	}
	return nil, &NotLoadedError{edge: "site_memberships"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Site) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case site.FieldPublic:
			values[i] = new(sql.NullBool)
		case site.FieldID, site.FieldIngestRateLimitScaleSeconds, site.FieldIngestLimitPerMinute:
			values[i] = new(sql.NullInt64)
		case site.FieldDomain, site.FieldTimezone:
			values[i] = new(sql.NullString)
		case site.FieldStatsStartDate, site.FieldCreatedAt, site.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Site fields.
func (s *Site) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case site.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			s.ID = int64(value.Int64)
		case site.FieldDomain:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field domain", values[i])
			} else if value.Valid {
				s.Domain = value.String
			}
		case site.FieldTimezone:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field timezone", values[i])
			} else if value.Valid {
				s.Timezone = value.String
			}
		case site.FieldPublic:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field public", values[i])
			} else if value.Valid {
				s.Public = value.Bool
			}
		case site.FieldStatsStartDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field stats_start_date", values[i])
			} else if value.Valid {
				s.StatsStartDate = new(time.Time)
				*s.StatsStartDate = value.Time
			}
		case site.FieldIngestRateLimitScaleSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ingest_rate_limit_scale_seconds", values[i])
			} else if value.Valid {
				s.IngestRateLimitScaleSeconds = int(value.Int64)
			}
		case site.FieldIngestLimitPerMinute:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ingest_limit_per_minute", values[i])
			} else if value.Valid {
				s.IngestLimitPerMinute = int(value.Int64)
			}
		case site.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				s.CreatedAt = value.Time
			}
		case site.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				s.UpdatedAt = value.Time
			}
		default:
			s.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Site.
// This includes values selected through modifiers, order, etc.
func (s *Site) Value(name string) (ent.Value, error) {
	return s.selectValues.Get(name)
}

// QueryFunnels queries the "funnels" edge of the Site entity.
func (s *Site) QueryFunnels() *FunnelQuery {
	return NewSiteClient(s.config).QueryFunnels(s)
}

// QueryMembers queries the "members" edge of the Site entity.
func (s *Site) QueryMembers() *UserQuery {
	return NewSiteClient(s.config).QueryMembers(s)
}

// QueryGoals queries the "goals" edge of the Site entity.
func (s *Site) QueryGoals() *GoalQuery {
	return NewSiteClient(s.config).QueryGoals(s)
}

// QuerySiteMemberships queries the "site_memberships" edge of the Site entity.
func (s *Site) QuerySiteMemberships() *SiteMembershipQuery {
	return NewSiteClient(s.config).QuerySiteMemberships(s)
}

// Update returns a builder for updating this Site.
// Note that you need to call Site.Unwrap() before calling this method if this Site
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Site) Update() *SiteUpdateOne {
	return NewSiteClient(s.config).UpdateOne(s)
}

// Unwrap unwraps the Site entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (s *Site) Unwrap() *Site {
	_tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Site is not a transactional entity")
	}
	s.config.driver = _tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Site) String() string {
	var builder strings.Builder
	builder.WriteString("Site(")
	builder.WriteString(fmt.Sprintf("id=%v, ", s.ID))
	builder.WriteString("domain=")
	builder.WriteString(s.Domain)
	builder.WriteString(", ")
	builder.WriteString("timezone=")
	builder.WriteString(s.Timezone)
	builder.WriteString(", ")
	builder.WriteString("public=")
	builder.WriteString(fmt.Sprintf("%v", s.Public))
	builder.WriteString(", ")
	if v := s.StatsStartDate; v != nil {
		builder.WriteString("stats_start_date=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("ingest_rate_limit_scale_seconds=")
	builder.WriteString(fmt.Sprintf("%v", s.IngestRateLimitScaleSeconds))
	builder.WriteString(", ")
	builder.WriteString("ingest_limit_per_minute=")
	builder.WriteString(fmt.Sprintf("%v", s.IngestLimitPerMinute))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(s.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(s.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Sites is a parsable slice of Site.
type Sites []*Site
