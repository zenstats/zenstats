// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/predicate"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/shieldrulescountry"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// ShieldRulesCountryQuery is the builder for querying ShieldRulesCountry entities.
type ShieldRulesCountryQuery struct {
	config
	ctx        *QueryContext
	order      []shieldrulescountry.OrderOption
	inters     []Interceptor
	predicates []predicate.ShieldRulesCountry
	withSite   *SiteQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShieldRulesCountryQuery builder.
func (srcq *ShieldRulesCountryQuery) Where(ps ...predicate.ShieldRulesCountry) *ShieldRulesCountryQuery {
	srcq.predicates = append(srcq.predicates, ps...)
	return srcq
}

// Limit the number of records to be returned by this query.
func (srcq *ShieldRulesCountryQuery) Limit(limit int) *ShieldRulesCountryQuery {
	srcq.ctx.Limit = &limit
	return srcq
}

// Offset to start from.
func (srcq *ShieldRulesCountryQuery) Offset(offset int) *ShieldRulesCountryQuery {
	srcq.ctx.Offset = &offset
	return srcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srcq *ShieldRulesCountryQuery) Unique(unique bool) *ShieldRulesCountryQuery {
	srcq.ctx.Unique = &unique
	return srcq
}

// Order specifies how the records should be ordered.
func (srcq *ShieldRulesCountryQuery) Order(o ...shieldrulescountry.OrderOption) *ShieldRulesCountryQuery {
	srcq.order = append(srcq.order, o...)
	return srcq
}

// QuerySite chains the current query on the "site" edge.
func (srcq *ShieldRulesCountryQuery) QuerySite() *SiteQuery {
	query := (&SiteClient{config: srcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shieldrulescountry.Table, shieldrulescountry.FieldID, selector),
			sqlgraph.To(site.Table, site.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shieldrulescountry.SiteTable, shieldrulescountry.SiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(srcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShieldRulesCountry entity from the query.
// Returns a *NotFoundError when no ShieldRulesCountry was found.
func (srcq *ShieldRulesCountryQuery) First(ctx context.Context) (*ShieldRulesCountry, error) {
	nodes, err := srcq.Limit(1).All(setContextOp(ctx, srcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shieldrulescountry.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) FirstX(ctx context.Context) *ShieldRulesCountry {
	node, err := srcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShieldRulesCountry ID from the query.
// Returns a *NotFoundError when no ShieldRulesCountry ID was found.
func (srcq *ShieldRulesCountryQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = srcq.Limit(1).IDs(setContextOp(ctx, srcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shieldrulescountry.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) FirstIDX(ctx context.Context) int64 {
	id, err := srcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShieldRulesCountry entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShieldRulesCountry entity is found.
// Returns a *NotFoundError when no ShieldRulesCountry entities are found.
func (srcq *ShieldRulesCountryQuery) Only(ctx context.Context) (*ShieldRulesCountry, error) {
	nodes, err := srcq.Limit(2).All(setContextOp(ctx, srcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shieldrulescountry.Label}
	default:
		return nil, &NotSingularError{shieldrulescountry.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) OnlyX(ctx context.Context) *ShieldRulesCountry {
	node, err := srcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShieldRulesCountry ID in the query.
// Returns a *NotSingularError when more than one ShieldRulesCountry ID is found.
// Returns a *NotFoundError when no entities are found.
func (srcq *ShieldRulesCountryQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = srcq.Limit(2).IDs(setContextOp(ctx, srcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shieldrulescountry.Label}
	default:
		err = &NotSingularError{shieldrulescountry.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := srcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShieldRulesCountries.
func (srcq *ShieldRulesCountryQuery) All(ctx context.Context) ([]*ShieldRulesCountry, error) {
	ctx = setContextOp(ctx, srcq.ctx, ent.OpQueryAll)
	if err := srcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShieldRulesCountry, *ShieldRulesCountryQuery]()
	return withInterceptors[[]*ShieldRulesCountry](ctx, srcq, qr, srcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) AllX(ctx context.Context) []*ShieldRulesCountry {
	nodes, err := srcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShieldRulesCountry IDs.
func (srcq *ShieldRulesCountryQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if srcq.ctx.Unique == nil && srcq.path != nil {
		srcq.Unique(true)
	}
	ctx = setContextOp(ctx, srcq.ctx, ent.OpQueryIDs)
	if err = srcq.Select(shieldrulescountry.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) IDsX(ctx context.Context) []int64 {
	ids, err := srcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srcq *ShieldRulesCountryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srcq.ctx, ent.OpQueryCount)
	if err := srcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srcq, querierCount[*ShieldRulesCountryQuery](), srcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) CountX(ctx context.Context) int {
	count, err := srcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srcq *ShieldRulesCountryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srcq.ctx, ent.OpQueryExist)
	switch _, err := srcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srcq *ShieldRulesCountryQuery) ExistX(ctx context.Context) bool {
	exist, err := srcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShieldRulesCountryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srcq *ShieldRulesCountryQuery) Clone() *ShieldRulesCountryQuery {
	if srcq == nil {
		return nil
	}
	return &ShieldRulesCountryQuery{
		config:     srcq.config,
		ctx:        srcq.ctx.Clone(),
		order:      append([]shieldrulescountry.OrderOption{}, srcq.order...),
		inters:     append([]Interceptor{}, srcq.inters...),
		predicates: append([]predicate.ShieldRulesCountry{}, srcq.predicates...),
		withSite:   srcq.withSite.Clone(),
		// clone intermediate query.
		sql:  srcq.sql.Clone(),
		path: srcq.path,
	}
}

// WithSite tells the query-builder to eager-load the nodes that are connected to
// the "site" edge. The optional arguments are used to configure the query builder of the edge.
func (srcq *ShieldRulesCountryQuery) WithSite(opts ...func(*SiteQuery)) *ShieldRulesCountryQuery {
	query := (&SiteClient{config: srcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srcq.withSite = query
	return srcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShieldRulesCountry.Query().
//		GroupBy(shieldrulescountry.FieldSiteID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (srcq *ShieldRulesCountryQuery) GroupBy(field string, fields ...string) *ShieldRulesCountryGroupBy {
	srcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShieldRulesCountryGroupBy{build: srcq}
	grbuild.flds = &srcq.ctx.Fields
	grbuild.label = shieldrulescountry.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//	}
//
//	client.ShieldRulesCountry.Query().
//		Select(shieldrulescountry.FieldSiteID).
//		Scan(ctx, &v)
func (srcq *ShieldRulesCountryQuery) Select(fields ...string) *ShieldRulesCountrySelect {
	srcq.ctx.Fields = append(srcq.ctx.Fields, fields...)
	sbuild := &ShieldRulesCountrySelect{ShieldRulesCountryQuery: srcq}
	sbuild.label = shieldrulescountry.Label
	sbuild.flds, sbuild.scan = &srcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShieldRulesCountrySelect configured with the given aggregations.
func (srcq *ShieldRulesCountryQuery) Aggregate(fns ...AggregateFunc) *ShieldRulesCountrySelect {
	return srcq.Select().Aggregate(fns...)
}

func (srcq *ShieldRulesCountryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srcq); err != nil {
				return err
			}
		}
	}
	for _, f := range srcq.ctx.Fields {
		if !shieldrulescountry.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if srcq.path != nil {
		prev, err := srcq.path(ctx)
		if err != nil {
			return err
		}
		srcq.sql = prev
	}
	return nil
}

func (srcq *ShieldRulesCountryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShieldRulesCountry, error) {
	var (
		nodes       = []*ShieldRulesCountry{}
		_spec       = srcq.querySpec()
		loadedTypes = [1]bool{
			srcq.withSite != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShieldRulesCountry).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShieldRulesCountry{config: srcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srcq.withSite; query != nil {
		if err := srcq.loadSite(ctx, query, nodes, nil,
			func(n *ShieldRulesCountry, e *Site) { n.Edges.Site = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srcq *ShieldRulesCountryQuery) loadSite(ctx context.Context, query *SiteQuery, nodes []*ShieldRulesCountry, init func(*ShieldRulesCountry), assign func(*ShieldRulesCountry, *Site)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ShieldRulesCountry)
	for i := range nodes {
		fk := nodes[i].SiteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(site.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srcq *ShieldRulesCountryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srcq.querySpec()
	_spec.Node.Columns = srcq.ctx.Fields
	if len(srcq.ctx.Fields) > 0 {
		_spec.Unique = srcq.ctx.Unique != nil && *srcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srcq.driver, _spec)
}

func (srcq *ShieldRulesCountryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shieldrulescountry.Table, shieldrulescountry.Columns, sqlgraph.NewFieldSpec(shieldrulescountry.FieldID, field.TypeInt64))
	_spec.From = srcq.sql
	if unique := srcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srcq.path != nil {
		_spec.Unique = true
	}
	if fields := srcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shieldrulescountry.FieldID)
		for i := range fields {
			if fields[i] != shieldrulescountry.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if srcq.withSite != nil {
			_spec.Node.AddColumnOnce(shieldrulescountry.FieldSiteID)
		}
	}
	if ps := srcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srcq *ShieldRulesCountryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srcq.driver.Dialect())
	t1 := builder.Table(shieldrulescountry.Table)
	columns := srcq.ctx.Fields
	if len(columns) == 0 {
		columns = shieldrulescountry.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srcq.sql != nil {
		selector = srcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srcq.ctx.Unique != nil && *srcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range srcq.predicates {
		p(selector)
	}
	for _, p := range srcq.order {
		p(selector)
	}
	if offset := srcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShieldRulesCountryGroupBy is the group-by builder for ShieldRulesCountry entities.
type ShieldRulesCountryGroupBy struct {
	selector
	build *ShieldRulesCountryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srcgb *ShieldRulesCountryGroupBy) Aggregate(fns ...AggregateFunc) *ShieldRulesCountryGroupBy {
	srcgb.fns = append(srcgb.fns, fns...)
	return srcgb
}

// Scan applies the selector query and scans the result into the given value.
func (srcgb *ShieldRulesCountryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srcgb.build.ctx, ent.OpQueryGroupBy)
	if err := srcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesCountryQuery, *ShieldRulesCountryGroupBy](ctx, srcgb.build, srcgb, srcgb.build.inters, v)
}

func (srcgb *ShieldRulesCountryGroupBy) sqlScan(ctx context.Context, root *ShieldRulesCountryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srcgb.fns))
	for _, fn := range srcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srcgb.flds)+len(srcgb.fns))
		for _, f := range *srcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShieldRulesCountrySelect is the builder for selecting fields of ShieldRulesCountry entities.
type ShieldRulesCountrySelect struct {
	*ShieldRulesCountryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srcs *ShieldRulesCountrySelect) Aggregate(fns ...AggregateFunc) *ShieldRulesCountrySelect {
	srcs.fns = append(srcs.fns, fns...)
	return srcs
}

// Scan applies the selector query and scans the result into the given value.
func (srcs *ShieldRulesCountrySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srcs.ctx, ent.OpQuerySelect)
	if err := srcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesCountryQuery, *ShieldRulesCountrySelect](ctx, srcs.ShieldRulesCountryQuery, srcs, srcs.inters, v)
}

func (srcs *ShieldRulesCountrySelect) sqlScan(ctx context.Context, root *ShieldRulesCountryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srcs.fns))
	for _, fn := range srcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
