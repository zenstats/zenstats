// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/predicate"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/shieldrulesip"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// ShieldRulesIpQuery is the builder for querying ShieldRulesIp entities.
type ShieldRulesIpQuery struct {
	config
	ctx        *QueryContext
	order      []shieldrulesip.OrderOption
	inters     []Interceptor
	predicates []predicate.ShieldRulesIp
	withSite   *SiteQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShieldRulesIpQuery builder.
func (sriq *ShieldRulesIpQuery) Where(ps ...predicate.ShieldRulesIp) *ShieldRulesIpQuery {
	sriq.predicates = append(sriq.predicates, ps...)
	return sriq
}

// Limit the number of records to be returned by this query.
func (sriq *ShieldRulesIpQuery) Limit(limit int) *ShieldRulesIpQuery {
	sriq.ctx.Limit = &limit
	return sriq
}

// Offset to start from.
func (sriq *ShieldRulesIpQuery) Offset(offset int) *ShieldRulesIpQuery {
	sriq.ctx.Offset = &offset
	return sriq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sriq *ShieldRulesIpQuery) Unique(unique bool) *ShieldRulesIpQuery {
	sriq.ctx.Unique = &unique
	return sriq
}

// Order specifies how the records should be ordered.
func (sriq *ShieldRulesIpQuery) Order(o ...shieldrulesip.OrderOption) *ShieldRulesIpQuery {
	sriq.order = append(sriq.order, o...)
	return sriq
}

// QuerySite chains the current query on the "site" edge.
func (sriq *ShieldRulesIpQuery) QuerySite() *SiteQuery {
	query := (&SiteClient{config: sriq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sriq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sriq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shieldrulesip.Table, shieldrulesip.FieldID, selector),
			sqlgraph.To(site.Table, site.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shieldrulesip.SiteTable, shieldrulesip.SiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(sriq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShieldRulesIp entity from the query.
// Returns a *NotFoundError when no ShieldRulesIp was found.
func (sriq *ShieldRulesIpQuery) First(ctx context.Context) (*ShieldRulesIp, error) {
	nodes, err := sriq.Limit(1).All(setContextOp(ctx, sriq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shieldrulesip.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) FirstX(ctx context.Context) *ShieldRulesIp {
	node, err := sriq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShieldRulesIp ID from the query.
// Returns a *NotFoundError when no ShieldRulesIp ID was found.
func (sriq *ShieldRulesIpQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = sriq.Limit(1).IDs(setContextOp(ctx, sriq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shieldrulesip.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) FirstIDX(ctx context.Context) int64 {
	id, err := sriq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShieldRulesIp entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShieldRulesIp entity is found.
// Returns a *NotFoundError when no ShieldRulesIp entities are found.
func (sriq *ShieldRulesIpQuery) Only(ctx context.Context) (*ShieldRulesIp, error) {
	nodes, err := sriq.Limit(2).All(setContextOp(ctx, sriq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shieldrulesip.Label}
	default:
		return nil, &NotSingularError{shieldrulesip.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) OnlyX(ctx context.Context) *ShieldRulesIp {
	node, err := sriq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShieldRulesIp ID in the query.
// Returns a *NotSingularError when more than one ShieldRulesIp ID is found.
// Returns a *NotFoundError when no entities are found.
func (sriq *ShieldRulesIpQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = sriq.Limit(2).IDs(setContextOp(ctx, sriq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shieldrulesip.Label}
	default:
		err = &NotSingularError{shieldrulesip.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := sriq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShieldRulesIps.
func (sriq *ShieldRulesIpQuery) All(ctx context.Context) ([]*ShieldRulesIp, error) {
	ctx = setContextOp(ctx, sriq.ctx, ent.OpQueryAll)
	if err := sriq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShieldRulesIp, *ShieldRulesIpQuery]()
	return withInterceptors[[]*ShieldRulesIp](ctx, sriq, qr, sriq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) AllX(ctx context.Context) []*ShieldRulesIp {
	nodes, err := sriq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShieldRulesIp IDs.
func (sriq *ShieldRulesIpQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if sriq.ctx.Unique == nil && sriq.path != nil {
		sriq.Unique(true)
	}
	ctx = setContextOp(ctx, sriq.ctx, ent.OpQueryIDs)
	if err = sriq.Select(shieldrulesip.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) IDsX(ctx context.Context) []int64 {
	ids, err := sriq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sriq *ShieldRulesIpQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sriq.ctx, ent.OpQueryCount)
	if err := sriq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sriq, querierCount[*ShieldRulesIpQuery](), sriq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) CountX(ctx context.Context) int {
	count, err := sriq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sriq *ShieldRulesIpQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sriq.ctx, ent.OpQueryExist)
	switch _, err := sriq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sriq *ShieldRulesIpQuery) ExistX(ctx context.Context) bool {
	exist, err := sriq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShieldRulesIpQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sriq *ShieldRulesIpQuery) Clone() *ShieldRulesIpQuery {
	if sriq == nil {
		return nil
	}
	return &ShieldRulesIpQuery{
		config:     sriq.config,
		ctx:        sriq.ctx.Clone(),
		order:      append([]shieldrulesip.OrderOption{}, sriq.order...),
		inters:     append([]Interceptor{}, sriq.inters...),
		predicates: append([]predicate.ShieldRulesIp{}, sriq.predicates...),
		withSite:   sriq.withSite.Clone(),
		// clone intermediate query.
		sql:  sriq.sql.Clone(),
		path: sriq.path,
	}
}

// WithSite tells the query-builder to eager-load the nodes that are connected to
// the "site" edge. The optional arguments are used to configure the query builder of the edge.
func (sriq *ShieldRulesIpQuery) WithSite(opts ...func(*SiteQuery)) *ShieldRulesIpQuery {
	query := (&SiteClient{config: sriq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sriq.withSite = query
	return sriq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShieldRulesIp.Query().
//		GroupBy(shieldrulesip.FieldSiteID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sriq *ShieldRulesIpQuery) GroupBy(field string, fields ...string) *ShieldRulesIpGroupBy {
	sriq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShieldRulesIpGroupBy{build: sriq}
	grbuild.flds = &sriq.ctx.Fields
	grbuild.label = shieldrulesip.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//	}
//
//	client.ShieldRulesIp.Query().
//		Select(shieldrulesip.FieldSiteID).
//		Scan(ctx, &v)
func (sriq *ShieldRulesIpQuery) Select(fields ...string) *ShieldRulesIpSelect {
	sriq.ctx.Fields = append(sriq.ctx.Fields, fields...)
	sbuild := &ShieldRulesIpSelect{ShieldRulesIpQuery: sriq}
	sbuild.label = shieldrulesip.Label
	sbuild.flds, sbuild.scan = &sriq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShieldRulesIpSelect configured with the given aggregations.
func (sriq *ShieldRulesIpQuery) Aggregate(fns ...AggregateFunc) *ShieldRulesIpSelect {
	return sriq.Select().Aggregate(fns...)
}

func (sriq *ShieldRulesIpQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sriq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sriq); err != nil {
				return err
			}
		}
	}
	for _, f := range sriq.ctx.Fields {
		if !shieldrulesip.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sriq.path != nil {
		prev, err := sriq.path(ctx)
		if err != nil {
			return err
		}
		sriq.sql = prev
	}
	return nil
}

func (sriq *ShieldRulesIpQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShieldRulesIp, error) {
	var (
		nodes       = []*ShieldRulesIp{}
		_spec       = sriq.querySpec()
		loadedTypes = [1]bool{
			sriq.withSite != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShieldRulesIp).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShieldRulesIp{config: sriq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sriq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sriq.withSite; query != nil {
		if err := sriq.loadSite(ctx, query, nodes, nil,
			func(n *ShieldRulesIp, e *Site) { n.Edges.Site = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sriq *ShieldRulesIpQuery) loadSite(ctx context.Context, query *SiteQuery, nodes []*ShieldRulesIp, init func(*ShieldRulesIp), assign func(*ShieldRulesIp, *Site)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ShieldRulesIp)
	for i := range nodes {
		fk := nodes[i].SiteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(site.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sriq *ShieldRulesIpQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sriq.querySpec()
	_spec.Node.Columns = sriq.ctx.Fields
	if len(sriq.ctx.Fields) > 0 {
		_spec.Unique = sriq.ctx.Unique != nil && *sriq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sriq.driver, _spec)
}

func (sriq *ShieldRulesIpQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shieldrulesip.Table, shieldrulesip.Columns, sqlgraph.NewFieldSpec(shieldrulesip.FieldID, field.TypeInt64))
	_spec.From = sriq.sql
	if unique := sriq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sriq.path != nil {
		_spec.Unique = true
	}
	if fields := sriq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shieldrulesip.FieldID)
		for i := range fields {
			if fields[i] != shieldrulesip.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sriq.withSite != nil {
			_spec.Node.AddColumnOnce(shieldrulesip.FieldSiteID)
		}
	}
	if ps := sriq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sriq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sriq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sriq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sriq *ShieldRulesIpQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sriq.driver.Dialect())
	t1 := builder.Table(shieldrulesip.Table)
	columns := sriq.ctx.Fields
	if len(columns) == 0 {
		columns = shieldrulesip.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sriq.sql != nil {
		selector = sriq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sriq.ctx.Unique != nil && *sriq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sriq.predicates {
		p(selector)
	}
	for _, p := range sriq.order {
		p(selector)
	}
	if offset := sriq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sriq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShieldRulesIpGroupBy is the group-by builder for ShieldRulesIp entities.
type ShieldRulesIpGroupBy struct {
	selector
	build *ShieldRulesIpQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srigb *ShieldRulesIpGroupBy) Aggregate(fns ...AggregateFunc) *ShieldRulesIpGroupBy {
	srigb.fns = append(srigb.fns, fns...)
	return srigb
}

// Scan applies the selector query and scans the result into the given value.
func (srigb *ShieldRulesIpGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srigb.build.ctx, ent.OpQueryGroupBy)
	if err := srigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesIpQuery, *ShieldRulesIpGroupBy](ctx, srigb.build, srigb, srigb.build.inters, v)
}

func (srigb *ShieldRulesIpGroupBy) sqlScan(ctx context.Context, root *ShieldRulesIpQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srigb.fns))
	for _, fn := range srigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srigb.flds)+len(srigb.fns))
		for _, f := range *srigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShieldRulesIpSelect is the builder for selecting fields of ShieldRulesIp entities.
type ShieldRulesIpSelect struct {
	*ShieldRulesIpQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sris *ShieldRulesIpSelect) Aggregate(fns ...AggregateFunc) *ShieldRulesIpSelect {
	sris.fns = append(sris.fns, fns...)
	return sris
}

// Scan applies the selector query and scans the result into the given value.
func (sris *ShieldRulesIpSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sris.ctx, ent.OpQuerySelect)
	if err := sris.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesIpQuery, *ShieldRulesIpSelect](ctx, sris.ShieldRulesIpQuery, sris, sris.inters, v)
}

func (sris *ShieldRulesIpSelect) sqlScan(ctx context.Context, root *ShieldRulesIpQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sris.fns))
	for _, fn := range sris.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sris.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sris.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
