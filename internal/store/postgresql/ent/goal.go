// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/goal"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// Goal is the model entity for the Goal schema.
type Goal struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// SiteID holds the value of the "site_id" field.
	SiteID int64 `json:"site_id,omitempty"`
	// EventName holds the value of the "event_name" field.
	EventName string `json:"event_name,omitempty"`
	// PagePath holds the value of the "page_path" field.
	PagePath string `json:"page_path,omitempty"`
	// DisplayName holds the value of the "display_name" field.
	DisplayName string `json:"display_name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GoalQuery when eager-loading is set.
	Edges        GoalEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GoalEdges holds the relations/edges for other nodes in the graph.
type GoalEdges struct {
	// Site holds the value of the site edge.
	Site *Site `json:"site,omitempty"`
	// FunnelSteps holds the value of the funnel_steps edge.
	FunnelSteps []*FunnelStep `json:"funnel_steps,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SiteOrErr returns the Site value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GoalEdges) SiteOrErr() (*Site, error) {
	if e.Site != nil {
		return e.Site, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: site.Label}
	}
	return nil, &NotLoadedError{edge: "site"}
}

// FunnelStepsOrErr returns the FunnelSteps value or an error if the edge
// was not loaded in eager-loading.
func (e GoalEdges) FunnelStepsOrErr() ([]*FunnelStep, error) {
	if e.loadedTypes[1] {
		return e.FunnelSteps, nil
	}
	return nil, &NotLoadedError{edge: "funnel_steps"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Goal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case goal.FieldID, goal.FieldSiteID:
			values[i] = new(sql.NullInt64)
		case goal.FieldEventName, goal.FieldPagePath, goal.FieldDisplayName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Goal fields.
func (_go *Goal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case goal.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_go.ID = int64(value.Int64)
		case goal.FieldSiteID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field site_id", values[i])
			} else if value.Valid {
				_go.SiteID = value.Int64
			}
		case goal.FieldEventName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_name", values[i])
			} else if value.Valid {
				_go.EventName = value.String
			}
		case goal.FieldPagePath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field page_path", values[i])
			} else if value.Valid {
				_go.PagePath = value.String
			}
		case goal.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				_go.DisplayName = value.String
			}
		default:
			_go.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Goal.
// This includes values selected through modifiers, order, etc.
func (_go *Goal) Value(name string) (ent.Value, error) {
	return _go.selectValues.Get(name)
}

// QuerySite queries the "site" edge of the Goal entity.
func (_go *Goal) QuerySite() *SiteQuery {
	return NewGoalClient(_go.config).QuerySite(_go)
}

// QueryFunnelSteps queries the "funnel_steps" edge of the Goal entity.
func (_go *Goal) QueryFunnelSteps() *FunnelStepQuery {
	return NewGoalClient(_go.config).QueryFunnelSteps(_go)
}

// Update returns a builder for updating this Goal.
// Note that you need to call Goal.Unwrap() before calling this method if this Goal
// was returned from a transaction, and the transaction was committed or rolled back.
func (_go *Goal) Update() *GoalUpdateOne {
	return NewGoalClient(_go.config).UpdateOne(_go)
}

// Unwrap unwraps the Goal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_go *Goal) Unwrap() *Goal {
	_tx, ok := _go.config.driver.(*txDriver)
	if !ok {
		panic("ent: Goal is not a transactional entity")
	}
	_go.config.driver = _tx.drv
	return _go
}

// String implements the fmt.Stringer.
func (_go *Goal) String() string {
	var builder strings.Builder
	builder.WriteString("Goal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _go.ID))
	builder.WriteString("site_id=")
	builder.WriteString(fmt.Sprintf("%v", _go.SiteID))
	builder.WriteString(", ")
	builder.WriteString("event_name=")
	builder.WriteString(_go.EventName)
	builder.WriteString(", ")
	builder.WriteString("page_path=")
	builder.WriteString(_go.PagePath)
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(_go.DisplayName)
	builder.WriteByte(')')
	return builder.String()
}

// Goals is a parsable slice of Goal.
type Goals []*Goal
