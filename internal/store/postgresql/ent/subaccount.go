// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/subaccount"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/user"
)

// SubAccount is the model entity for the SubAccount schema.
type SubAccount struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// 父用户ID
	ParentUserID int64 `json:"parent_user_id,omitempty"`
	// 子账号邮箱
	Email string `json:"email,omitempty"`
	// 密码哈希
	PasswordHash string `json:"password_hash,omitempty"`
	// 名称
	Name string `json:"name,omitempty"`
	// 角色：viewer（只读）
	Role string `json:"role,omitempty"`
	// 状态：active, suspended
	Status string `json:"status,omitempty"`
	// 最后登录时间
	LastSeen time.Time `json:"last_seen,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SubAccountQuery when eager-loading is set.
	Edges        SubAccountEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SubAccountEdges holds the relations/edges for other nodes in the graph.
type SubAccountEdges struct {
	// ParentUser holds the value of the parent_user edge.
	ParentUser *User `json:"parent_user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ParentUserOrErr returns the ParentUser value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SubAccountEdges) ParentUserOrErr() (*User, error) {
	if e.ParentUser != nil {
		return e.ParentUser, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "parent_user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SubAccount) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case subaccount.FieldID, subaccount.FieldParentUserID:
			values[i] = new(sql.NullInt64)
		case subaccount.FieldEmail, subaccount.FieldPasswordHash, subaccount.FieldName, subaccount.FieldRole, subaccount.FieldStatus:
			values[i] = new(sql.NullString)
		case subaccount.FieldLastSeen, subaccount.FieldCreatedAt, subaccount.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SubAccount fields.
func (sa *SubAccount) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case subaccount.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sa.ID = int64(value.Int64)
		case subaccount.FieldParentUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field parent_user_id", values[i])
			} else if value.Valid {
				sa.ParentUserID = value.Int64
			}
		case subaccount.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				sa.Email = value.String
			}
		case subaccount.FieldPasswordHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password_hash", values[i])
			} else if value.Valid {
				sa.PasswordHash = value.String
			}
		case subaccount.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				sa.Name = value.String
			}
		case subaccount.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				sa.Role = value.String
			}
		case subaccount.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				sa.Status = value.String
			}
		case subaccount.FieldLastSeen:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen", values[i])
			} else if value.Valid {
				sa.LastSeen = value.Time
			}
		case subaccount.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sa.CreatedAt = value.Time
			}
		case subaccount.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sa.UpdatedAt = value.Time
			}
		default:
			sa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SubAccount.
// This includes values selected through modifiers, order, etc.
func (sa *SubAccount) Value(name string) (ent.Value, error) {
	return sa.selectValues.Get(name)
}

// QueryParentUser queries the "parent_user" edge of the SubAccount entity.
func (sa *SubAccount) QueryParentUser() *UserQuery {
	return NewSubAccountClient(sa.config).QueryParentUser(sa)
}

// Update returns a builder for updating this SubAccount.
// Note that you need to call SubAccount.Unwrap() before calling this method if this SubAccount
// was returned from a transaction, and the transaction was committed or rolled back.
func (sa *SubAccount) Update() *SubAccountUpdateOne {
	return NewSubAccountClient(sa.config).UpdateOne(sa)
}

// Unwrap unwraps the SubAccount entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sa *SubAccount) Unwrap() *SubAccount {
	_tx, ok := sa.config.driver.(*txDriver)
	if !ok {
		panic("ent: SubAccount is not a transactional entity")
	}
	sa.config.driver = _tx.drv
	return sa
}

// String implements the fmt.Stringer.
func (sa *SubAccount) String() string {
	var builder strings.Builder
	builder.WriteString("SubAccount(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sa.ID))
	builder.WriteString("parent_user_id=")
	builder.WriteString(fmt.Sprintf("%v", sa.ParentUserID))
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(sa.Email)
	builder.WriteString(", ")
	builder.WriteString("password_hash=")
	builder.WriteString(sa.PasswordHash)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(sa.Name)
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(sa.Role)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(sa.Status)
	builder.WriteString(", ")
	builder.WriteString("last_seen=")
	builder.WriteString(sa.LastSeen.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sa.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sa.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SubAccounts is a parsable slice of SubAccount.
type SubAccounts []*SubAccount
