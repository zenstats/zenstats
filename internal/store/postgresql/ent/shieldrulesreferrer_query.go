// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/predicate"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/shieldrulesreferrer"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// ShieldRulesReferrerQuery is the builder for querying ShieldRulesReferrer entities.
type ShieldRulesReferrerQuery struct {
	config
	ctx        *QueryContext
	order      []shieldrulesreferrer.OrderOption
	inters     []Interceptor
	predicates []predicate.ShieldRulesReferrer
	withSite   *SiteQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShieldRulesReferrerQuery builder.
func (srrq *ShieldRulesReferrerQuery) Where(ps ...predicate.ShieldRulesReferrer) *ShieldRulesReferrerQuery {
	srrq.predicates = append(srrq.predicates, ps...)
	return srrq
}

// Limit the number of records to be returned by this query.
func (srrq *ShieldRulesReferrerQuery) Limit(limit int) *ShieldRulesReferrerQuery {
	srrq.ctx.Limit = &limit
	return srrq
}

// Offset to start from.
func (srrq *ShieldRulesReferrerQuery) Offset(offset int) *ShieldRulesReferrerQuery {
	srrq.ctx.Offset = &offset
	return srrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srrq *ShieldRulesReferrerQuery) Unique(unique bool) *ShieldRulesReferrerQuery {
	srrq.ctx.Unique = &unique
	return srrq
}

// Order specifies how the records should be ordered.
func (srrq *ShieldRulesReferrerQuery) Order(o ...shieldrulesreferrer.OrderOption) *ShieldRulesReferrerQuery {
	srrq.order = append(srrq.order, o...)
	return srrq
}

// QuerySite chains the current query on the "site" edge.
func (srrq *ShieldRulesReferrerQuery) QuerySite() *SiteQuery {
	query := (&SiteClient{config: srrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shieldrulesreferrer.Table, shieldrulesreferrer.FieldID, selector),
			sqlgraph.To(site.Table, site.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shieldrulesreferrer.SiteTable, shieldrulesreferrer.SiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(srrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShieldRulesReferrer entity from the query.
// Returns a *NotFoundError when no ShieldRulesReferrer was found.
func (srrq *ShieldRulesReferrerQuery) First(ctx context.Context) (*ShieldRulesReferrer, error) {
	nodes, err := srrq.Limit(1).All(setContextOp(ctx, srrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shieldrulesreferrer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) FirstX(ctx context.Context) *ShieldRulesReferrer {
	node, err := srrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShieldRulesReferrer ID from the query.
// Returns a *NotFoundError when no ShieldRulesReferrer ID was found.
func (srrq *ShieldRulesReferrerQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = srrq.Limit(1).IDs(setContextOp(ctx, srrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shieldrulesreferrer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) FirstIDX(ctx context.Context) int64 {
	id, err := srrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShieldRulesReferrer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShieldRulesReferrer entity is found.
// Returns a *NotFoundError when no ShieldRulesReferrer entities are found.
func (srrq *ShieldRulesReferrerQuery) Only(ctx context.Context) (*ShieldRulesReferrer, error) {
	nodes, err := srrq.Limit(2).All(setContextOp(ctx, srrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shieldrulesreferrer.Label}
	default:
		return nil, &NotSingularError{shieldrulesreferrer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) OnlyX(ctx context.Context) *ShieldRulesReferrer {
	node, err := srrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShieldRulesReferrer ID in the query.
// Returns a *NotSingularError when more than one ShieldRulesReferrer ID is found.
// Returns a *NotFoundError when no entities are found.
func (srrq *ShieldRulesReferrerQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = srrq.Limit(2).IDs(setContextOp(ctx, srrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shieldrulesreferrer.Label}
	default:
		err = &NotSingularError{shieldrulesreferrer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := srrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShieldRulesReferrers.
func (srrq *ShieldRulesReferrerQuery) All(ctx context.Context) ([]*ShieldRulesReferrer, error) {
	ctx = setContextOp(ctx, srrq.ctx, ent.OpQueryAll)
	if err := srrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShieldRulesReferrer, *ShieldRulesReferrerQuery]()
	return withInterceptors[[]*ShieldRulesReferrer](ctx, srrq, qr, srrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) AllX(ctx context.Context) []*ShieldRulesReferrer {
	nodes, err := srrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShieldRulesReferrer IDs.
func (srrq *ShieldRulesReferrerQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if srrq.ctx.Unique == nil && srrq.path != nil {
		srrq.Unique(true)
	}
	ctx = setContextOp(ctx, srrq.ctx, ent.OpQueryIDs)
	if err = srrq.Select(shieldrulesreferrer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) IDsX(ctx context.Context) []int64 {
	ids, err := srrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srrq *ShieldRulesReferrerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srrq.ctx, ent.OpQueryCount)
	if err := srrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srrq, querierCount[*ShieldRulesReferrerQuery](), srrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) CountX(ctx context.Context) int {
	count, err := srrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srrq *ShieldRulesReferrerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srrq.ctx, ent.OpQueryExist)
	switch _, err := srrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srrq *ShieldRulesReferrerQuery) ExistX(ctx context.Context) bool {
	exist, err := srrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShieldRulesReferrerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srrq *ShieldRulesReferrerQuery) Clone() *ShieldRulesReferrerQuery {
	if srrq == nil {
		return nil
	}
	return &ShieldRulesReferrerQuery{
		config:     srrq.config,
		ctx:        srrq.ctx.Clone(),
		order:      append([]shieldrulesreferrer.OrderOption{}, srrq.order...),
		inters:     append([]Interceptor{}, srrq.inters...),
		predicates: append([]predicate.ShieldRulesReferrer{}, srrq.predicates...),
		withSite:   srrq.withSite.Clone(),
		// clone intermediate query.
		sql:  srrq.sql.Clone(),
		path: srrq.path,
	}
}

// WithSite tells the query-builder to eager-load the nodes that are connected to
// the "site" edge. The optional arguments are used to configure the query builder of the edge.
func (srrq *ShieldRulesReferrerQuery) WithSite(opts ...func(*SiteQuery)) *ShieldRulesReferrerQuery {
	query := (&SiteClient{config: srrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srrq.withSite = query
	return srrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShieldRulesReferrer.Query().
//		GroupBy(shieldrulesreferrer.FieldSiteID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (srrq *ShieldRulesReferrerQuery) GroupBy(field string, fields ...string) *ShieldRulesReferrerGroupBy {
	srrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShieldRulesReferrerGroupBy{build: srrq}
	grbuild.flds = &srrq.ctx.Fields
	grbuild.label = shieldrulesreferrer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//	}
//
//	client.ShieldRulesReferrer.Query().
//		Select(shieldrulesreferrer.FieldSiteID).
//		Scan(ctx, &v)
func (srrq *ShieldRulesReferrerQuery) Select(fields ...string) *ShieldRulesReferrerSelect {
	srrq.ctx.Fields = append(srrq.ctx.Fields, fields...)
	sbuild := &ShieldRulesReferrerSelect{ShieldRulesReferrerQuery: srrq}
	sbuild.label = shieldrulesreferrer.Label
	sbuild.flds, sbuild.scan = &srrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShieldRulesReferrerSelect configured with the given aggregations.
func (srrq *ShieldRulesReferrerQuery) Aggregate(fns ...AggregateFunc) *ShieldRulesReferrerSelect {
	return srrq.Select().Aggregate(fns...)
}

func (srrq *ShieldRulesReferrerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srrq); err != nil {
				return err
			}
		}
	}
	for _, f := range srrq.ctx.Fields {
		if !shieldrulesreferrer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if srrq.path != nil {
		prev, err := srrq.path(ctx)
		if err != nil {
			return err
		}
		srrq.sql = prev
	}
	return nil
}

func (srrq *ShieldRulesReferrerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShieldRulesReferrer, error) {
	var (
		nodes       = []*ShieldRulesReferrer{}
		_spec       = srrq.querySpec()
		loadedTypes = [1]bool{
			srrq.withSite != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShieldRulesReferrer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShieldRulesReferrer{config: srrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srrq.withSite; query != nil {
		if err := srrq.loadSite(ctx, query, nodes, nil,
			func(n *ShieldRulesReferrer, e *Site) { n.Edges.Site = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srrq *ShieldRulesReferrerQuery) loadSite(ctx context.Context, query *SiteQuery, nodes []*ShieldRulesReferrer, init func(*ShieldRulesReferrer), assign func(*ShieldRulesReferrer, *Site)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ShieldRulesReferrer)
	for i := range nodes {
		fk := nodes[i].SiteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(site.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srrq *ShieldRulesReferrerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srrq.querySpec()
	_spec.Node.Columns = srrq.ctx.Fields
	if len(srrq.ctx.Fields) > 0 {
		_spec.Unique = srrq.ctx.Unique != nil && *srrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srrq.driver, _spec)
}

func (srrq *ShieldRulesReferrerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shieldrulesreferrer.Table, shieldrulesreferrer.Columns, sqlgraph.NewFieldSpec(shieldrulesreferrer.FieldID, field.TypeInt64))
	_spec.From = srrq.sql
	if unique := srrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srrq.path != nil {
		_spec.Unique = true
	}
	if fields := srrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shieldrulesreferrer.FieldID)
		for i := range fields {
			if fields[i] != shieldrulesreferrer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if srrq.withSite != nil {
			_spec.Node.AddColumnOnce(shieldrulesreferrer.FieldSiteID)
		}
	}
	if ps := srrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srrq *ShieldRulesReferrerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srrq.driver.Dialect())
	t1 := builder.Table(shieldrulesreferrer.Table)
	columns := srrq.ctx.Fields
	if len(columns) == 0 {
		columns = shieldrulesreferrer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srrq.sql != nil {
		selector = srrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srrq.ctx.Unique != nil && *srrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range srrq.predicates {
		p(selector)
	}
	for _, p := range srrq.order {
		p(selector)
	}
	if offset := srrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShieldRulesReferrerGroupBy is the group-by builder for ShieldRulesReferrer entities.
type ShieldRulesReferrerGroupBy struct {
	selector
	build *ShieldRulesReferrerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srrgb *ShieldRulesReferrerGroupBy) Aggregate(fns ...AggregateFunc) *ShieldRulesReferrerGroupBy {
	srrgb.fns = append(srrgb.fns, fns...)
	return srrgb
}

// Scan applies the selector query and scans the result into the given value.
func (srrgb *ShieldRulesReferrerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srrgb.build.ctx, ent.OpQueryGroupBy)
	if err := srrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesReferrerQuery, *ShieldRulesReferrerGroupBy](ctx, srrgb.build, srrgb, srrgb.build.inters, v)
}

func (srrgb *ShieldRulesReferrerGroupBy) sqlScan(ctx context.Context, root *ShieldRulesReferrerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srrgb.fns))
	for _, fn := range srrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srrgb.flds)+len(srrgb.fns))
		for _, f := range *srrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShieldRulesReferrerSelect is the builder for selecting fields of ShieldRulesReferrer entities.
type ShieldRulesReferrerSelect struct {
	*ShieldRulesReferrerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srrs *ShieldRulesReferrerSelect) Aggregate(fns ...AggregateFunc) *ShieldRulesReferrerSelect {
	srrs.fns = append(srrs.fns, fns...)
	return srrs
}

// Scan applies the selector query and scans the result into the given value.
func (srrs *ShieldRulesReferrerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srrs.ctx, ent.OpQuerySelect)
	if err := srrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesReferrerQuery, *ShieldRulesReferrerSelect](ctx, srrs.ShieldRulesReferrerQuery, srrs, srrs.inters, v)
}

func (srrs *ShieldRulesReferrerSelect) sqlScan(ctx context.Context, root *ShieldRulesReferrerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srrs.fns))
	for _, fn := range srrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
