// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/funnel"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/funnelstep"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// FunnelCreate is the builder for creating a Funnel entity.
type FunnelCreate struct {
	config
	mutation *FunnelMutation
	hooks    []Hook
}

// SetSiteID sets the "site_id" field.
func (fc *FunnelCreate) SetSiteID(i int64) *FunnelCreate {
	fc.mutation.SetSiteID(i)
	return fc
}

// SetName sets the "name" field.
func (fc *FunnelCreate) SetName(s string) *FunnelCreate {
	fc.mutation.SetName(s)
	return fc
}

// SetID sets the "id" field.
func (fc *FunnelCreate) SetID(i int64) *FunnelCreate {
	fc.mutation.SetID(i)
	return fc
}

// SetSite sets the "site" edge to the Site entity.
func (fc *FunnelCreate) SetSite(s *Site) *FunnelCreate {
	return fc.SetSiteID(s.ID)
}

// AddFunnelStepIDs adds the "funnel_steps" edge to the FunnelStep entity by IDs.
func (fc *FunnelCreate) AddFunnelStepIDs(ids ...int64) *FunnelCreate {
	fc.mutation.AddFunnelStepIDs(ids...)
	return fc
}

// AddFunnelSteps adds the "funnel_steps" edges to the FunnelStep entity.
func (fc *FunnelCreate) AddFunnelSteps(f ...*FunnelStep) *FunnelCreate {
	ids := make([]int64, len(f))
	for i := range f {
		ids[i] = f[i].ID
	}
	return fc.AddFunnelStepIDs(ids...)
}

// Mutation returns the FunnelMutation object of the builder.
func (fc *FunnelCreate) Mutation() *FunnelMutation {
	return fc.mutation
}

// Save creates the Funnel in the database.
func (fc *FunnelCreate) Save(ctx context.Context) (*Funnel, error) {
	return withHooks(ctx, fc.sqlSave, fc.mutation, fc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (fc *FunnelCreate) SaveX(ctx context.Context) *Funnel {
	v, err := fc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fc *FunnelCreate) Exec(ctx context.Context) error {
	_, err := fc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fc *FunnelCreate) ExecX(ctx context.Context) {
	if err := fc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (fc *FunnelCreate) check() error {
	if _, ok := fc.mutation.SiteID(); !ok {
		return &ValidationError{Name: "site_id", err: errors.New(`ent: missing required field "Funnel.site_id"`)}
	}
	if _, ok := fc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Funnel.name"`)}
	}
	if v, ok := fc.mutation.Name(); ok {
		if err := funnel.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "Funnel.name": %w`, err)}
		}
	}
	if len(fc.mutation.SiteIDs()) == 0 {
		return &ValidationError{Name: "site", err: errors.New(`ent: missing required edge "Funnel.site"`)}
	}
	return nil
}

func (fc *FunnelCreate) sqlSave(ctx context.Context) (*Funnel, error) {
	if err := fc.check(); err != nil {
		return nil, err
	}
	_node, _spec := fc.createSpec()
	if err := sqlgraph.CreateNode(ctx, fc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int64(id)
	}
	fc.mutation.id = &_node.ID
	fc.mutation.done = true
	return _node, nil
}

func (fc *FunnelCreate) createSpec() (*Funnel, *sqlgraph.CreateSpec) {
	var (
		_node = &Funnel{config: fc.config}
		_spec = sqlgraph.NewCreateSpec(funnel.Table, sqlgraph.NewFieldSpec(funnel.FieldID, field.TypeInt64))
	)
	if id, ok := fc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := fc.mutation.Name(); ok {
		_spec.SetField(funnel.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if nodes := fc.mutation.SiteIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   funnel.SiteTable,
			Columns: []string{funnel.SiteColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(site.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.SiteID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := fc.mutation.FunnelStepsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   funnel.FunnelStepsTable,
			Columns: []string{funnel.FunnelStepsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(funnelstep.FieldID, field.TypeInt64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// FunnelCreateBulk is the builder for creating many Funnel entities in bulk.
type FunnelCreateBulk struct {
	config
	err      error
	builders []*FunnelCreate
}

// Save creates the Funnel entities in the database.
func (fcb *FunnelCreateBulk) Save(ctx context.Context) ([]*Funnel, error) {
	if fcb.err != nil {
		return nil, fcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(fcb.builders))
	nodes := make([]*Funnel, len(fcb.builders))
	mutators := make([]Mutator, len(fcb.builders))
	for i := range fcb.builders {
		func(i int, root context.Context) {
			builder := fcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*FunnelMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, fcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, fcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, fcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (fcb *FunnelCreateBulk) SaveX(ctx context.Context) []*Funnel {
	v, err := fcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fcb *FunnelCreateBulk) Exec(ctx context.Context) error {
	_, err := fcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fcb *FunnelCreateBulk) ExecX(ctx context.Context) {
	if err := fcb.Exec(ctx); err != nil {
		panic(err)
	}
}
