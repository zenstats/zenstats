// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/sharedlink"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// SharedLink is the model entity for the SharedLink schema.
type SharedLink struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// SiteID holds the value of the "site_id" field.
	SiteID int64 `json:"site_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Slug holds the value of the "slug" field.
	Slug string `json:"slug,omitempty"`
	// PasswordHash holds the value of the "password_hash" field.
	PasswordHash string `json:"password_hash,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SharedLinkQuery when eager-loading is set.
	Edges        SharedLinkEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SharedLinkEdges holds the relations/edges for other nodes in the graph.
type SharedLinkEdges struct {
	// Site holds the value of the site edge.
	Site *Site `json:"site,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SiteOrErr returns the Site value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SharedLinkEdges) SiteOrErr() (*Site, error) {
	if e.Site != nil {
		return e.Site, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: site.Label}
	}
	return nil, &NotLoadedError{edge: "site"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SharedLink) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sharedlink.FieldID, sharedlink.FieldSiteID:
			values[i] = new(sql.NullInt64)
		case sharedlink.FieldName, sharedlink.FieldSlug, sharedlink.FieldPasswordHash:
			values[i] = new(sql.NullString)
		case sharedlink.FieldCreatedAt, sharedlink.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SharedLink fields.
func (sl *SharedLink) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sharedlink.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sl.ID = int64(value.Int64)
		case sharedlink.FieldSiteID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field site_id", values[i])
			} else if value.Valid {
				sl.SiteID = value.Int64
			}
		case sharedlink.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				sl.Name = value.String
			}
		case sharedlink.FieldSlug:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field slug", values[i])
			} else if value.Valid {
				sl.Slug = value.String
			}
		case sharedlink.FieldPasswordHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password_hash", values[i])
			} else if value.Valid {
				sl.PasswordHash = value.String
			}
		case sharedlink.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sl.CreatedAt = value.Time
			}
		case sharedlink.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sl.UpdatedAt = value.Time
			}
		default:
			sl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SharedLink.
// This includes values selected through modifiers, order, etc.
func (sl *SharedLink) Value(name string) (ent.Value, error) {
	return sl.selectValues.Get(name)
}

// QuerySite queries the "site" edge of the SharedLink entity.
func (sl *SharedLink) QuerySite() *SiteQuery {
	return NewSharedLinkClient(sl.config).QuerySite(sl)
}

// Update returns a builder for updating this SharedLink.
// Note that you need to call SharedLink.Unwrap() before calling this method if this SharedLink
// was returned from a transaction, and the transaction was committed or rolled back.
func (sl *SharedLink) Update() *SharedLinkUpdateOne {
	return NewSharedLinkClient(sl.config).UpdateOne(sl)
}

// Unwrap unwraps the SharedLink entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sl *SharedLink) Unwrap() *SharedLink {
	_tx, ok := sl.config.driver.(*txDriver)
	if !ok {
		panic("ent: SharedLink is not a transactional entity")
	}
	sl.config.driver = _tx.drv
	return sl
}

// String implements the fmt.Stringer.
func (sl *SharedLink) String() string {
	var builder strings.Builder
	builder.WriteString("SharedLink(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sl.ID))
	builder.WriteString("site_id=")
	builder.WriteString(fmt.Sprintf("%v", sl.SiteID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(sl.Name)
	builder.WriteString(", ")
	builder.WriteString("slug=")
	builder.WriteString(sl.Slug)
	builder.WriteString(", ")
	builder.WriteString("password_hash=")
	builder.WriteString(sl.PasswordHash)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sl.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SharedLinks is a parsable slice of SharedLink.
type SharedLinks []*SharedLink
