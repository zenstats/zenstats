// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/customsearchengine"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/user"
)

// CustomSearchEngine is the model entity for the CustomSearchEngine schema.
type CustomSearchEngine struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// 所属用户ID
	UserID int64 `json:"user_id,omitempty"`
	// 搜索引擎域名，如 google.com
	Domain string `json:"domain,omitempty"`
	// 显示名称，如 Google
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CustomSearchEngineQuery when eager-loading is set.
	Edges        CustomSearchEngineEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CustomSearchEngineEdges holds the relations/edges for other nodes in the graph.
type CustomSearchEngineEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CustomSearchEngineEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CustomSearchEngine) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case customsearchengine.FieldID, customsearchengine.FieldUserID:
			values[i] = new(sql.NullInt64)
		case customsearchengine.FieldDomain, customsearchengine.FieldName:
			values[i] = new(sql.NullString)
		case customsearchengine.FieldCreatedAt, customsearchengine.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CustomSearchEngine fields.
func (cse *CustomSearchEngine) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case customsearchengine.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cse.ID = int64(value.Int64)
		case customsearchengine.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				cse.UserID = value.Int64
			}
		case customsearchengine.FieldDomain:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field domain", values[i])
			} else if value.Valid {
				cse.Domain = value.String
			}
		case customsearchengine.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				cse.Name = value.String
			}
		case customsearchengine.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				cse.CreatedAt = value.Time
			}
		case customsearchengine.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				cse.UpdatedAt = value.Time
			}
		default:
			cse.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CustomSearchEngine.
// This includes values selected through modifiers, order, etc.
func (cse *CustomSearchEngine) Value(name string) (ent.Value, error) {
	return cse.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the CustomSearchEngine entity.
func (cse *CustomSearchEngine) QueryUser() *UserQuery {
	return NewCustomSearchEngineClient(cse.config).QueryUser(cse)
}

// Update returns a builder for updating this CustomSearchEngine.
// Note that you need to call CustomSearchEngine.Unwrap() before calling this method if this CustomSearchEngine
// was returned from a transaction, and the transaction was committed or rolled back.
func (cse *CustomSearchEngine) Update() *CustomSearchEngineUpdateOne {
	return NewCustomSearchEngineClient(cse.config).UpdateOne(cse)
}

// Unwrap unwraps the CustomSearchEngine entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cse *CustomSearchEngine) Unwrap() *CustomSearchEngine {
	_tx, ok := cse.config.driver.(*txDriver)
	if !ok {
		panic("ent: CustomSearchEngine is not a transactional entity")
	}
	cse.config.driver = _tx.drv
	return cse
}

// String implements the fmt.Stringer.
func (cse *CustomSearchEngine) String() string {
	var builder strings.Builder
	builder.WriteString("CustomSearchEngine(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cse.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", cse.UserID))
	builder.WriteString(", ")
	builder.WriteString("domain=")
	builder.WriteString(cse.Domain)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(cse.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(cse.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(cse.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CustomSearchEngines is a parsable slice of CustomSearchEngine.
type CustomSearchEngines []*CustomSearchEngine
