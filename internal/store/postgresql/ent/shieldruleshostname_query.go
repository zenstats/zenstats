// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/predicate"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/shieldruleshostname"
	"github.com/zenstats/zenstats/internal/store/postgresql/ent/site"
)

// ShieldRulesHostnameQuery is the builder for querying ShieldRulesHostname entities.
type ShieldRulesHostnameQuery struct {
	config
	ctx        *QueryContext
	order      []shieldruleshostname.OrderOption
	inters     []Interceptor
	predicates []predicate.ShieldRulesHostname
	withSite   *SiteQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShieldRulesHostnameQuery builder.
func (srhq *ShieldRulesHostnameQuery) Where(ps ...predicate.ShieldRulesHostname) *ShieldRulesHostnameQuery {
	srhq.predicates = append(srhq.predicates, ps...)
	return srhq
}

// Limit the number of records to be returned by this query.
func (srhq *ShieldRulesHostnameQuery) Limit(limit int) *ShieldRulesHostnameQuery {
	srhq.ctx.Limit = &limit
	return srhq
}

// Offset to start from.
func (srhq *ShieldRulesHostnameQuery) Offset(offset int) *ShieldRulesHostnameQuery {
	srhq.ctx.Offset = &offset
	return srhq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srhq *ShieldRulesHostnameQuery) Unique(unique bool) *ShieldRulesHostnameQuery {
	srhq.ctx.Unique = &unique
	return srhq
}

// Order specifies how the records should be ordered.
func (srhq *ShieldRulesHostnameQuery) Order(o ...shieldruleshostname.OrderOption) *ShieldRulesHostnameQuery {
	srhq.order = append(srhq.order, o...)
	return srhq
}

// QuerySite chains the current query on the "site" edge.
func (srhq *ShieldRulesHostnameQuery) QuerySite() *SiteQuery {
	query := (&SiteClient{config: srhq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := srhq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := srhq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shieldruleshostname.Table, shieldruleshostname.FieldID, selector),
			sqlgraph.To(site.Table, site.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shieldruleshostname.SiteTable, shieldruleshostname.SiteColumn),
		)
		fromU = sqlgraph.SetNeighbors(srhq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShieldRulesHostname entity from the query.
// Returns a *NotFoundError when no ShieldRulesHostname was found.
func (srhq *ShieldRulesHostnameQuery) First(ctx context.Context) (*ShieldRulesHostname, error) {
	nodes, err := srhq.Limit(1).All(setContextOp(ctx, srhq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shieldruleshostname.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) FirstX(ctx context.Context) *ShieldRulesHostname {
	node, err := srhq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShieldRulesHostname ID from the query.
// Returns a *NotFoundError when no ShieldRulesHostname ID was found.
func (srhq *ShieldRulesHostnameQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = srhq.Limit(1).IDs(setContextOp(ctx, srhq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shieldruleshostname.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) FirstIDX(ctx context.Context) int64 {
	id, err := srhq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShieldRulesHostname entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShieldRulesHostname entity is found.
// Returns a *NotFoundError when no ShieldRulesHostname entities are found.
func (srhq *ShieldRulesHostnameQuery) Only(ctx context.Context) (*ShieldRulesHostname, error) {
	nodes, err := srhq.Limit(2).All(setContextOp(ctx, srhq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shieldruleshostname.Label}
	default:
		return nil, &NotSingularError{shieldruleshostname.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) OnlyX(ctx context.Context) *ShieldRulesHostname {
	node, err := srhq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShieldRulesHostname ID in the query.
// Returns a *NotSingularError when more than one ShieldRulesHostname ID is found.
// Returns a *NotFoundError when no entities are found.
func (srhq *ShieldRulesHostnameQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = srhq.Limit(2).IDs(setContextOp(ctx, srhq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shieldruleshostname.Label}
	default:
		err = &NotSingularError{shieldruleshostname.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := srhq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShieldRulesHostnames.
func (srhq *ShieldRulesHostnameQuery) All(ctx context.Context) ([]*ShieldRulesHostname, error) {
	ctx = setContextOp(ctx, srhq.ctx, ent.OpQueryAll)
	if err := srhq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShieldRulesHostname, *ShieldRulesHostnameQuery]()
	return withInterceptors[[]*ShieldRulesHostname](ctx, srhq, qr, srhq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) AllX(ctx context.Context) []*ShieldRulesHostname {
	nodes, err := srhq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShieldRulesHostname IDs.
func (srhq *ShieldRulesHostnameQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if srhq.ctx.Unique == nil && srhq.path != nil {
		srhq.Unique(true)
	}
	ctx = setContextOp(ctx, srhq.ctx, ent.OpQueryIDs)
	if err = srhq.Select(shieldruleshostname.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) IDsX(ctx context.Context) []int64 {
	ids, err := srhq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srhq *ShieldRulesHostnameQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srhq.ctx, ent.OpQueryCount)
	if err := srhq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srhq, querierCount[*ShieldRulesHostnameQuery](), srhq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) CountX(ctx context.Context) int {
	count, err := srhq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srhq *ShieldRulesHostnameQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srhq.ctx, ent.OpQueryExist)
	switch _, err := srhq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srhq *ShieldRulesHostnameQuery) ExistX(ctx context.Context) bool {
	exist, err := srhq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShieldRulesHostnameQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srhq *ShieldRulesHostnameQuery) Clone() *ShieldRulesHostnameQuery {
	if srhq == nil {
		return nil
	}
	return &ShieldRulesHostnameQuery{
		config:     srhq.config,
		ctx:        srhq.ctx.Clone(),
		order:      append([]shieldruleshostname.OrderOption{}, srhq.order...),
		inters:     append([]Interceptor{}, srhq.inters...),
		predicates: append([]predicate.ShieldRulesHostname{}, srhq.predicates...),
		withSite:   srhq.withSite.Clone(),
		// clone intermediate query.
		sql:  srhq.sql.Clone(),
		path: srhq.path,
	}
}

// WithSite tells the query-builder to eager-load the nodes that are connected to
// the "site" edge. The optional arguments are used to configure the query builder of the edge.
func (srhq *ShieldRulesHostnameQuery) WithSite(opts ...func(*SiteQuery)) *ShieldRulesHostnameQuery {
	query := (&SiteClient{config: srhq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	srhq.withSite = query
	return srhq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShieldRulesHostname.Query().
//		GroupBy(shieldruleshostname.FieldSiteID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (srhq *ShieldRulesHostnameQuery) GroupBy(field string, fields ...string) *ShieldRulesHostnameGroupBy {
	srhq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShieldRulesHostnameGroupBy{build: srhq}
	grbuild.flds = &srhq.ctx.Fields
	grbuild.label = shieldruleshostname.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SiteID int64 `json:"site_id,omitempty"`
//	}
//
//	client.ShieldRulesHostname.Query().
//		Select(shieldruleshostname.FieldSiteID).
//		Scan(ctx, &v)
func (srhq *ShieldRulesHostnameQuery) Select(fields ...string) *ShieldRulesHostnameSelect {
	srhq.ctx.Fields = append(srhq.ctx.Fields, fields...)
	sbuild := &ShieldRulesHostnameSelect{ShieldRulesHostnameQuery: srhq}
	sbuild.label = shieldruleshostname.Label
	sbuild.flds, sbuild.scan = &srhq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShieldRulesHostnameSelect configured with the given aggregations.
func (srhq *ShieldRulesHostnameQuery) Aggregate(fns ...AggregateFunc) *ShieldRulesHostnameSelect {
	return srhq.Select().Aggregate(fns...)
}

func (srhq *ShieldRulesHostnameQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srhq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srhq); err != nil {
				return err
			}
		}
	}
	for _, f := range srhq.ctx.Fields {
		if !shieldruleshostname.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if srhq.path != nil {
		prev, err := srhq.path(ctx)
		if err != nil {
			return err
		}
		srhq.sql = prev
	}
	return nil
}

func (srhq *ShieldRulesHostnameQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShieldRulesHostname, error) {
	var (
		nodes       = []*ShieldRulesHostname{}
		_spec       = srhq.querySpec()
		loadedTypes = [1]bool{
			srhq.withSite != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShieldRulesHostname).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShieldRulesHostname{config: srhq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srhq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := srhq.withSite; query != nil {
		if err := srhq.loadSite(ctx, query, nodes, nil,
			func(n *ShieldRulesHostname, e *Site) { n.Edges.Site = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (srhq *ShieldRulesHostnameQuery) loadSite(ctx context.Context, query *SiteQuery, nodes []*ShieldRulesHostname, init func(*ShieldRulesHostname), assign func(*ShieldRulesHostname, *Site)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ShieldRulesHostname)
	for i := range nodes {
		fk := nodes[i].SiteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(site.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "site_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (srhq *ShieldRulesHostnameQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srhq.querySpec()
	_spec.Node.Columns = srhq.ctx.Fields
	if len(srhq.ctx.Fields) > 0 {
		_spec.Unique = srhq.ctx.Unique != nil && *srhq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srhq.driver, _spec)
}

func (srhq *ShieldRulesHostnameQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shieldruleshostname.Table, shieldruleshostname.Columns, sqlgraph.NewFieldSpec(shieldruleshostname.FieldID, field.TypeInt64))
	_spec.From = srhq.sql
	if unique := srhq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srhq.path != nil {
		_spec.Unique = true
	}
	if fields := srhq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shieldruleshostname.FieldID)
		for i := range fields {
			if fields[i] != shieldruleshostname.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if srhq.withSite != nil {
			_spec.Node.AddColumnOnce(shieldruleshostname.FieldSiteID)
		}
	}
	if ps := srhq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srhq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srhq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srhq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srhq *ShieldRulesHostnameQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srhq.driver.Dialect())
	t1 := builder.Table(shieldruleshostname.Table)
	columns := srhq.ctx.Fields
	if len(columns) == 0 {
		columns = shieldruleshostname.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srhq.sql != nil {
		selector = srhq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srhq.ctx.Unique != nil && *srhq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range srhq.predicates {
		p(selector)
	}
	for _, p := range srhq.order {
		p(selector)
	}
	if offset := srhq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srhq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ShieldRulesHostnameGroupBy is the group-by builder for ShieldRulesHostname entities.
type ShieldRulesHostnameGroupBy struct {
	selector
	build *ShieldRulesHostnameQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srhgb *ShieldRulesHostnameGroupBy) Aggregate(fns ...AggregateFunc) *ShieldRulesHostnameGroupBy {
	srhgb.fns = append(srhgb.fns, fns...)
	return srhgb
}

// Scan applies the selector query and scans the result into the given value.
func (srhgb *ShieldRulesHostnameGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srhgb.build.ctx, ent.OpQueryGroupBy)
	if err := srhgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesHostnameQuery, *ShieldRulesHostnameGroupBy](ctx, srhgb.build, srhgb, srhgb.build.inters, v)
}

func (srhgb *ShieldRulesHostnameGroupBy) sqlScan(ctx context.Context, root *ShieldRulesHostnameQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srhgb.fns))
	for _, fn := range srhgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srhgb.flds)+len(srhgb.fns))
		for _, f := range *srhgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srhgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srhgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShieldRulesHostnameSelect is the builder for selecting fields of ShieldRulesHostname entities.
type ShieldRulesHostnameSelect struct {
	*ShieldRulesHostnameQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srhs *ShieldRulesHostnameSelect) Aggregate(fns ...AggregateFunc) *ShieldRulesHostnameSelect {
	srhs.fns = append(srhs.fns, fns...)
	return srhs
}

// Scan applies the selector query and scans the result into the given value.
func (srhs *ShieldRulesHostnameSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srhs.ctx, ent.OpQuerySelect)
	if err := srhs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShieldRulesHostnameQuery, *ShieldRulesHostnameSelect](ctx, srhs.ShieldRulesHostnameQuery, srhs, srhs.inters, v)
}

func (srhs *ShieldRulesHostnameSelect) sqlScan(ctx context.Context, root *ShieldRulesHostnameQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srhs.fns))
	for _, fn := range srhs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srhs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srhs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
